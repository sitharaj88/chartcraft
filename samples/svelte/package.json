{
  "name": "@chartcraft/sample-svelte",
  "private": true,
  "version": "0.1.0",
  "description": "Northwind Cloud — a realistic analytics dashboard built with @chartcraft/svelte (Svelte + Vite + TypeScript)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "svelte-check --tsconfig ./tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@chartcraft/core": "^0.3.0",
    "@chartcraft/svelte": "^0.3.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
